import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Week2_mobile_cross-platform',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
